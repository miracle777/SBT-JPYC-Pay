# ウォレット接続デバッグガイド

## ブラウザのコンソールで確認する手順

### 1. ブラウザデベロッパーツール開く
- **Chrome/Edge**: `F12` キー
- **Firefox**: `F12` キー  
- **Safari**: `Cmd+Option+I`

### 2. Console タブを開く

### 3. ウォレット選択ボタンをクリック

### 4. コンソール出力を確認

以下のようなログが出力されているか確認してください：

```
🔍 ウォレット検出開始... { isMobile: false, isIOS: false, isAndroid: false, userAgent: ... }

ethereum: { exists: true/false, isMetaMask: true/false, isCoinbase: false, chainId: ... }

✅ ウォレット検出完了: { detected: 1, wallets: [{name: 'MetaMask', id: 'metamask', installed: true}] }

🔌 ウォレット選択: MetaMask ID: metamask

✅ 検出済みウォレットで接続開始

🔌 MetaMask での接続開始

[ここで MetaMask モーダルが表示されるはず]

⏱️ ウォレット接続タイムアウト または ✅ MetaMask 接続成功
```

## よくある問題

### 問題1: `ethereum: { exists: false }`
- MetaMask がインストール/有効化されていない
- **解決**: MetaMask ブラウザ拡張をインストールする

### 問題2: ウォレット検出は成功するが接続できない
- MetaMask モーダルが表示されない、または表示されたまま止まる
- **原因**: PWA のセキュリティ制限
- **解決**: 通常ブラウザで確認するか、MetaMask アプリのブラウザ機能を使用

### 問題3: 黒いモーダルが表示されたまま
- 公式接続 UI が応答していない
- **原因**: ネットワークの遅延、またはブラウザの制限
- **解決**: タイムアウト後に推奨ウォレット表示へフォールバック

## コンソール確認ポイント

1. **window.ethereum が存在するか**
   ```javascript
   window.ethereum
   ```

2. **MetaMask 検出状況**
   ```javascript
   window.ethereum?.isMetaMask
   ```

3. **複数プロバイダー確認**
   ```javascript
   window.ethereum?.providers?.length
   ```

4. **接続要求テスト**
   ```javascript
   window.ethereum?.request({ method: 'eth_requestAccounts' })
   ```

## 現在の実装

- 検出タイムアウト: **3秒（PWA）/ 2秒（通常ブラウザ）**
- 接続タイムアウト: **15秒**
- 推奨ウォレット: MetaMask, WalletConnect, Coinbase Wallet

